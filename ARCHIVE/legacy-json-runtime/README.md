# Retired JSON Runtime

This folder preserves the former JSON persistence adapters, file-backed event-journal helpers, and
their adapter-specific contract tests for historical inspection. They are not part of the active
runtime, supported development workflow, release checkpoint, or current verification evidence.

The active server requires PostgreSQL. Historical files here must not be cited as evidence of the
current implementation without explicitly identifying them as pre-cutover behavior.
